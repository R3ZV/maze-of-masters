import { IncomingMessage, OutgoingMessage } from './types';
export type WebPdWorkletNodeMessageHandler = (messageEvent: MessageEvent<IncomingMessage>) => void;
interface WebPdWorkletNodeMessagePort extends MessagePort {
    postMessage(message: OutgoingMessage, transfer: Transferable[]): void;
    postMessage(message: OutgoingMessage, options?: StructuredSerializeOptions): void;
    onmessage: (messageEvent: MessageEvent<IncomingMessage>) => void;
}
export default class WebPdWorkletNode extends AudioWorkletNode {
    port: WebPdWorkletNodeMessagePort;
    constructor(context: AudioContext);
    destroy(): void;
}
export declare const registerWebPdWorkletNode: (context: AudioContext) => Promise<void>;
export {};
