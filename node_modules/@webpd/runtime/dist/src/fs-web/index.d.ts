import WebPdWorkletNode from '../WebPdWorkletNode';
import { IncomingMessage } from '../types';
import { FsHandlerSettings } from './types';
declare const _default: (node: WebPdWorkletNode, messageEvent: MessageEvent<IncomingMessage>, settings: FsHandlerSettings) => Promise<void>;
export default _default;
