interface Settings {
    sampleRate: number;
    blockSize: number;
}
declare const FS_CALLBACK_NAMES: readonly ["onReadSoundFile", "onOpenSoundReadStream", "onWriteSoundFile", "onOpenSoundWriteStream", "onSoundStreamData", "onCloseSoundStream"];
declare class WasmWorkletProcessor extends AudioWorkletProcessor {
    private settings;
    private dspConfigured;
    private engine;
    constructor();
    process(inputs: Float32Array[][], outputs: Float32Array[][]): boolean;
    onMessage(messageEvent: MessageEvent<TypesForWorkletProcessor.OutgoingMessage>): void;
    setWasm(wasmBuffer: ArrayBuffer): Promise<void>;
    setJsCode(code: string): void;
    setEngine(engine: TypesForWorkletProcessor.Engine): void;
    destroy(): void;
}
